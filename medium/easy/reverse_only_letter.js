const s = "abcdef";
/*
function isValid(str){
    if(typeof(str) !== 'string'){       //typeof --> fct qui renvoit le type
        return (false);
    }
    for(let i = 0 ; i < str.length; i++){
        if(str.charCodeAt(i) < 33 || str.charCodeAt(i) > 122 || str[i] == '\"' || str[i] == '\\'){
            return (false);
        }
    }
    return (true);
}

function  reverse(valeur1){
    const size = valeur1.length;
    if (size < 1 || size > 100)
        return ("bad size");
    if (!isValid(valeur1))
        return ("char no print or bad char");
    return (valeur1.split("").reverse().join(""));
};
*/



const reverseOnlyLetters = s =>;

console.log(s);
console.log(reverseOnlyLetters);
